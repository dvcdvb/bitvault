spring:
  application:
    name: bitvault
  datasource:
    url: jdbc:h2:mem:bitvault
    username: sa
    password: password
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect